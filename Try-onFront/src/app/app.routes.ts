import { Routes } from '@angular/router';
import {TryOnComponent} from './try-on/try-on.component';

export const routes: Routes = [
  {path:'', component: TryOnComponent, title: 'Virtual Fashion Fit'},
];

export default routes;
